# Take an argument of a collection, iterate through that collection, then execute the code in the block you call it with for each element in the collection (use the yield keyword). It should return the modified collection.

